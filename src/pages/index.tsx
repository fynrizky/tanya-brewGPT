import Chat from "@/components/Chat"

export default function Home() {
  return (
    <main className={`bg-[#ededed] relative overflow-hidden rounded-1xl min-h-screen mx-auto ml-[35%] m-[0] mr-[35%] max-xl:mx-auto`}>
      <Chat />
    </main>
  )
}
